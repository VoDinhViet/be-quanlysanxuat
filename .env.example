PORT=8003
DATABASE_URL=postgresql://user:password@localhost:5432/db
DATABASE_SSL_MODE=false
FRONTEND_DOMAIN=http://localhost:3000
BACKEND_DOMAIN=http://localhost:8003
AUTH_CONFIRM_EMAIL_EXPIRES=24h
AUTH_JWT_SECRET=secret
AUTH_JWT_TOKEN_EXPIRES_IN=15m
AUTH_REFRESH_SECRET=refresh_secret
AUTH_REFRESH_TOKEN_EXPIRES_IN=7d
REDIS_URL=redis://localhost:6379
UPLOAD_DRIVER=local
UPLOAD_DIR=uploads
UPLOAD_MAX_IMAGE_SIZE=5242880
UPLOAD_MAX_DOCUMENT_SIZE=10485760
# REQUIRED (min 32 chars) — HMAC key for signed download URLs. File bytes are reachable only via a
# URL signed with this, so a weak or shared value means anyone can mint a valid download link.
# Generate with: openssl rand -base64 48
UPLOAD_URL_SECRET=change-me-to-a-long-random-string-min-32-chars
# How long a download URL stays valid, in seconds. Longer = friendlier caching, wider exposure.
UPLOAD_URL_TTL=3600
# Grace period before an uploaded-but-never-linked file is swept, in seconds. Must be longer than
# the longest realistic form-filling session.
UPLOAD_ORPHAN_TTL=86400
