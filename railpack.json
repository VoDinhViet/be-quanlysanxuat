{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "packages": {
    "node": "24"
  },
  "steps": {
    "install": {
      "inputs": [{ "local": true, "exclude": ["node_modules", "dist"] }],
      "commands": ["pnpm install --frozen-lockfile"]
    },
    "build": {
      "inputs": [{ "step": "install" }],
      "commands": ["pnpm run build"]
    }
  },
  "deploy": {
    "startCommand": "pnpm run start:prod",
    "inputs": [{ "step": "build" }]
  }
}
